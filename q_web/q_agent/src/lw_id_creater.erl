%%%--------------------------------------------------------------------------------------
%%% @author Zhangcongsong
%%% @copyright 2012-2013 livecom
%%% @doc Lwork id creater
%%% @end
%%%--------------------------------------------------------------------------------------
-module(lw_id_creater).
-compile(export_all).
-include("lw.hrl").

%%%-------------------------------------------------------------------------------------
%%% API
%%%-------------------------------------------------------------------------------------

%%%-------------------------------------------------------------------------------------
%%% internal functions
%%%-------------------------------------------------------------------------------------
generate_uuid() ->
    generate_lw_counter(uuid).

%%--------------------------------------------------------------------------------------

generate_taskid() ->
    generate_lw_counter(taskid).

%%--------------------------------------------------------------------------------------

generate_topicid() ->
    generate_lw_counter(topicid).

%%--------------------------------------------------------------------------------------

generate_pollid() ->
    generate_lw_counter(pollid).

%%--------------------------------------------------------------------------------------

generate_newsid() ->
    generate_lw_counter(newsid).

%%--------------------------------------------------------------------------------------

generate_documentid() ->
    generate_lw_counter(documentid).

%%--------------------------------------------------------------------------------------

generate_questionsid() ->
    generate_lw_counter(question).

%%--------------------------------------------------------------------------------------

generate_bbscatindex() ->
    generate_lw_counter(bbscat).

%%--------------------------------------------------------------------------------------

generate_bbsnoteid() ->
    generate_lw_counter(bbsnote).

%%--------------------------------------------------------------------------------------

generate_bbsnotereplyid() ->
    generate_lw_counter(bbsnotereply).

%%--------------------------------------------------------------------------------------

generate_imsessionid() ->
    generate_lw_counter(imsession).

%%--------------------------------------------------------------------------------------

generate_lw_counter(Type) ->
    mnesia:dirty_update_counter({lw_id_creater,Type},1).

%%--------------------------------------------------------------------------------------